# CLI Expense Tracker
# --------------------
# This project uses ONLY the Python standard library
# (dataclasses, json, logging, os, tempfile, datetime, pathlib,
# collections, statistics, typing) — no third-party packages
# are required to run it.
#
# Minimum Python version: 3.10
# (required for built-in union type hints like `str | None`)
#
# This file is kept intentionally empty of dependencies so the
# project remains zero-install. Add packages below if you extend
# the project, e.g.:
#
# pytest>=8.0.0        # for unit testing
# tabulate>=0.9.0       # for prettier table output
# openpyxl>=3.1.0        # for Excel export
